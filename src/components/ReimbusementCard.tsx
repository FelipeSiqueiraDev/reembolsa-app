import { reimbusementDTO } from "@dtos/reimbusementDTO";
import { Pressable, View } from "@gluestack-ui/themed";

export function ReimbusementCard(data: reimbusementDTO) {
  return (
    <Pressable w={"$full"} h={"$48"}>
      <View />
    </Pressable>
  );
}
